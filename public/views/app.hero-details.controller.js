angular
    .module("app.hero-detail.controller", [])
    .controller("app.hero-detail.controller", [
        "$scope",
        function ($scope) {
            $scope.viewName = "hero DETAIL";
        },
    ]);
